#include <stdio.h>

/**
 * main - prints the phrase and makes the code executable
 * Return: 0 if exited properly, otherwise non-zero
 */
 int main(void)
 {
           puts("\"Programming is like buiding a multilingual puzzle");
	   return (0);
 } 
